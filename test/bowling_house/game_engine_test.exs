defmodule BowlingHouse.GameEngineTest do
  use BowlingHouse.DataCase

  describe "roll/1" do
    test "four rolls are split by the game engine into two frames as expected" do
    end

    test "the game engine records two frames down when the player  makes three roles and the first roll is a strike" do
    end

    test "the game engine records two frames down when the player  makes a spare, then two rolls" do
    end
  end
end
